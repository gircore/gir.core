/// <summary>
/// Contains the name of the {{ $1.symbol_name}} property.
/// </summary>

{{~
$name = $1.symbol_name

if $name == "Properties"
    $name = "Properties_"
end
~}}
public const string {{ $name}} = "{{ $1.symbol_name }}";