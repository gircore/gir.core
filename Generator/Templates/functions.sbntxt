using System;
using System.Runtime.InteropServices;

#nullable enable

namespace {{ namespace.name }}
{
    public partial class Functions
    {
        //TODO this is temporarily public as long as there is no manged code available
        public static class Native
        {
            {{ include 'methods.sbntxt' functions ~}}
        }
    }
}