using System;
using System.Runtime.InteropServices;

namespace {{ namespace }}
{
    public partial class {{ name }}
    {
        {{~ for constructor in constructors
        include 'importable_method.sbntxt' constructor
        end ~}}

        {{~ for method in methods
        include 'importable_method.sbntxt' method
        end ~}}
    }
}